{
  "name": "discord-bot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "predev": "docker build -t nodelink-custom ./nodelink-src && docker rm -f nodelink & docker run -d --name nodelink -p 2333:2333 -e NODELINK_SERVER_PORT=2333 -e NODELINK_SERVER_PASSWORD=${NODELINK_PASSWORD:-youshallnotpass} nodelink-custom",
    "dev": "dotnet watch run --environment Development",
    "build": "dotnet build",
    "start": "dotnet run --environment Production",
    "publish": "dotnet publish -c Release -o dist"
  }
}
